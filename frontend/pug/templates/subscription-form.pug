mixin subscription-form(props = 0)
  include button
  include input-field
  include checkbox

  - const {otherClasses = false, sidebar = false} = props;

  - let formClassList = otherClasses ? 'subscription-form ' + otherClasses : 'subscription-form';
  - formClassList += sidebar ? ' subscription-form_sidebar' : '';


  if sidebar
    form(class= formClassList)
      fieldset.subscription-form__fieldset
        .container
          .row
            .col-12
              span.subscription-form__legend Подпишись и&nbsp;получай информацию о&nbsp;всех новинках и&nbsp;акциях!
              span.subscription-form__description.d-none.d-lg-block.
                Вы будете одними из первых
                узнавать о новых коллекциях, акциях
                и распродажах
            .col-12.col-md-4.col-lg-12
              +input-field({type: 'email', size: 'small', borderColor: 'transparent'})(inputmode='email' placeholder='E-mail', autocapitalize='off', autocorrect='off', autocomplete='email', required)
            .col-12.col-md-4.col-lg-12
              +input-field({type: 'text', size: 'small', borderColor: 'transparent'})( placeholder='Имя', autocapitalize='off', autocorrect='off', required)
            .col-12.col-md-4.col-lg-12
              +button({color: 'red', size: 'small', color: 'no-bg', borderColor: 'red'})
                |Подписаться
            .col-12
              +checkbox({labelColor: 'dark'})(checked=true require=true)
                |Нажимая кнопку «Подписаться», я даю свое согласие на обработку моих
                |
                |
                a(href="#" target="blank") персональных данных

  else
    form(class= formClassList)
      fieldset.subscription-form__fieldset
        .container
          .row
            .col-12.col-sm-8.col-md-6.col-lg-7.col-xl-6
              span.subscription-form__legend Подпишись и&nbsp;получай информацию о&nbsp;всех новинках и&nbsp;акциях!
            .col-12.col-md-6.col-lg-5.col-xl-6.col-xxl-5
              .subscription-form__input-wrapper
                +input-field({type: 'email', borderColor: 'transparent'})(inputmode='email' placeholder='Укажите Вашу почту', autocapitalize='off', autocorrect='off', autocomplete='email', required)
                +button({type: 'circle', size: 'small', color: 'red', iconId: '#mail'})
              +checkbox({labelColor: 'dark'})(checked=true require=ture)
                |Нажимая кнопку «Подписаться», я даю свое согласие на обработку моих
                |
                |
                a(href="#" target="blank") персональных данных
